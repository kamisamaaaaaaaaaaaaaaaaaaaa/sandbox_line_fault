# 2026-08-31

## fault-sandbox：配置 yml 化 + 迁移新验证机器

- **配置文件改 YAML**（用户要求）：config.properties → config.yml（嵌套分组格式），解析用 snakeyaml 1.33（common 依赖；**agent fat jar 必须 relocate org.yaml.snakeyaml**，因 Spring Boot 应用自带 snakeyaml 会与 system classpath 冲突）。FaultConfig.load 加载后把嵌套 Map 展平为点分键（jdbc.host 等 get() 键名零改动，同时兼容平面写法）
- **新验证机器**：lys@192.168.193.129（密码同 yi2000316lei，sudo 同），Ubuntu 20.04 OpenJDK 21，sandbox 在 **/home/lys/sandbox**（sandbox-module 单数）；MySQL 仍用 Windows 宿主 192.168.193.1:3306（root/root123，root@192.168.193.% 授权已覆盖 129），TCP 连通已验证。config.yml 的 sandbox.home 已改 /home/lys/sandbox 并重打上传（agent→/home/lys/、module→sandbox-module/、test-app→/home/lys/）
- plink 模板更新：`A:\putty\plink.exe -ssh -batch -pw yi2000316lei lys@192.168.193.129`（129 这台 host key 无需 -hostkey，echo y 已缓存）
- **新机器回归一次通过**：yml 配置生效 → 解析去重（unitId=2/3 completed，跨机器共享 Windows 库）→ 挂载 exit=0 → tagH 命中 TestApplication.<init> line=22 → kill ✓（commit de1b108）
- VMware 事件（8/31 上午）：用户开虚拟机转圈=vmware-vmx 僵死（log 0 字节/66MB 内存/命令行读不出）；taskkill/Stop-Process 均被 Access denied，**wmic process where processid=748 delete 成功**；清 3 个 .lck + 4GB 孤儿 vmem（ubuntu20.04）与 222 的 vmx.lck（新建文件夹）；ubuntu20.04-2 目录已不存在（inventory 失效条目）
- log.dir 日志目录配置（901fd2c）：FaultLogger.init(dir, fileName)（相对路径基于目标进程工作目录）；agent 侧 agentArgs/config 覆盖，挂载命令自动把 logDir 绝对路径传给 module（inject?id=x&logDir=...，module 侧 param 优先配置兜底）——agent/module 日志同目录；tagL2 回归通过（fault-logs/ 下双日志齐）✓
- halt(137) vs kill -9 行为对比结论：系统级一致（fd 关闭/TCP 终止/文件锁释放/子进程收养/无 core dump/无 shutdown hooks/退出码 137）；两处理论差异：waitpid 层面 halt 是 WEXITSTATUS(137) 而 kill -9 是 WIFSIGNALED(SIGKILL)（shell 的 $? 均为 137，仅精确解析 wait status 的进程管理器可区分）；halt 走 C exit 会 flush stdio（SIGKILL 直接丢弃）——对本工具无影响（日志每条显式落盘）
- 简化（ba042ed）：①删 logDir 传递——agent/module 日志目录各自独立（module 用自己 config.yml 的 log.dir，默认 logs），agent 不再替 module 传；②删 fired 内存判重（kill 后进程必死，防并发窗口意义极小），**保留 preemptedLines**——它拦的是"本节点存活+该行被其他节点占"的热路径反复撞库（QPS 1000 接口无缓存=每秒 1000 次无效 INSERT），非"进程将死"场景；正确性完全由 DB 唯一索引保证。tagM 回归通过
- 配置再精简（fa34622）：**删 jdbc.host，jdbc.url 必填**（require 强校验）；**lib.whitelist 升级正则**（逗号分隔多个正则、对 jar 文件名全串匹配 Pattern.matches、非法正则 warn 跳过）——注意全串匹配语义变化，前缀要写 `test-lib.*`；tagN 回归通过（正则 test-lib.* 命中既有 test-lib 单元）
- **保护缺口修复（7f9c0c5）**：①异机 pending 未完成时原实现直接跳过→B节点拉空表2放行裸奔；改为轮询等待（1s 间隔至 completed / failed 接手 / 孤儿接手，全程受 deadline 约束）；②module inject 查表2 为空从 warn 放行改为抛异常→sandbox 命令失败→agent 硬保护 kill；tagO 回归通过
- **错误全覆盖落表4（b79f74f）**：单 class 解析失败（ParseUnit.failedClasses→orchestrator 逐条写）、单类 watch 注册失败、beforeLine 任何异常、kill 失败——全部 phase=PARSE/INJECT 写 t_error_record；**kill 失败改为回滚表3 + 记表4 + 放行（不再 halt，更真实）**；agent 侧 hardProtect 的 kill 失败仍 halt（否则硬保护失效）
- **实测结论：inject 抛异常 agent 能感知**——sandbox.sh -d "fault-module/inject?id=99999" → EXIT=1（输出 target JVM lose response，目标 JVM 也被杀）→ agent 的 exitValue()!=0 分支触发硬保护 ✓
- **sandbox 1.4.0 API 事实**：`onBehavior(String)` 只收单参（无可变参数版），多方法必须逐个 `.onBehavior(m1).onBehavior(m2)...` 链式（当前实现）
- 排查技巧：PowerShell 传 plink 长命令时 `|`/`&`/`$()`/`awk` 会被本地解析，复杂命令应写成 .sh 上传后执行
- 模块自容错+分批（c69acc6）：①**模块内自行兜住所有致命异常**（缺 tag / 查表2 为空 / inject 任何异常 → 表4 + 模块内直接 kill），不再依赖"抛异常→sandbox.sh exit 1→agent kill"间接链路（实测该链路虽可行但有 "lose response" 副作用，行为不完全可控）；②方法清单改**游标分批读取** keyset 分页（id > lastId ORDER BY id LIMIT n），`inject.batch.size` 默认 5000，每批一个 listener 且注册后随批次释放（避免大项目全量方法入内存）；tagR 回归通过（日志含 batch registered: methods=26 classes=6 lastId=26）
- **构建坑**：改 resources/config.yml 后 gradle shadowJar 可能判 UP-TO-DATE 不重打包（产物与源码不一致）→ 用 `--rerun-tasks` 或先 clean；上传后应抽查 jar 内 config.yml 确认
- 超时拆分+排除清单（93d297e）：①删 premain.timeout.ms，拆为 **parse.timeout.ms（默认600000，含异机pending等待）** 与 **mount.timeout.ms（默认60000，独立计时）**；②module 加 **exclude.classes**（类名正则，整类不注入）/**exclude.methods**（"类名.方法名"正则），registerBatch 前过滤（少注册还省回调）；tagV 回归通过（日志 parseTimeout=600000 mountTimeout=60000）
- 排错教训：编译错误被 grep 过滤隐藏导致连续三轮用旧 jar 验证（实际 storeUnit 缺 config 参数 + orphanThresholdMinutes 被误删）——**构建后必须看 BUILD 状态行而非过滤关键字**
- 默认值调整+排除清单简化（53a68cb）：**parse.timeout.ms=900000（15min）、mount.timeout.ms=1200000（20min）**，代码默认值在 `FaultConfig.parseTimeoutMs()/mountTimeoutMs()`，config.yml 里显式写同值；**移除 exclude.classes**，只用 `exclude.methods`（正则全串匹配"全限定类名.方法名"），排除整类写 `cn\.demo\.OrderService\..*`
- 验证记录：tagW 轮临时把 exclude.methods 设为 `cn\.chinaclear\.fault\.testapp\..*` → 4 个 testapp 类整类跳过（registered=2/6），未匹配的 testlib.IdGenerator 照常注入并在 line=6 触发 kill，语义完全正确；tagZ 轮恢复空值回归 6/6

## 环境备忘（本机/远程）
- 库表实际带 `t_` 前缀：`t_jar_record / t_class_method / t_fault_record / t_error_record`（早期废弃表 jar_parse_status/jar_parse_result/fault_log 无前缀，别用错）
- **查库方式**：Windows 本机 mysql.exe 是 5.7 客户端，连 8.x 服务端报 caching_sha2_password 错；Linux 无 mysql 客户端。可行做法：在 Linux 上 `java -cp .:sandbox/sandbox-module/fault-module-1.0.0.jar Q`（module jar 内驱动未 relocate；agent jar 内 mysql 被 relocate 为 `cn.chinaclear.fault.shaded.mysql.cj`，不能直接用 com.mysql.cj.jdbc.Driver）
- plink 远程命令：**用 `plink -m 脚本文件`**，否则 PowerShell 会吞掉命令里的双引号（grep 参数被拆导致命令错乱）
