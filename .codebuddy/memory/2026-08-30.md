# 2026-08-30

## fault-sandbox 工程（sandbox行故障 workspace）JVM-Sandbox 故障注入工具

- 完成 Gradle 多模块工程（common/fault-agent/fault-module/test-lib/test-app），6 个 commit（至 e69f011）
- common：FaultConfig（classpath config.properties + agentArgs 覆盖）、SchemaInitializer（建库建 4 表幂等 DDL）、JdbcHelper（DriverManager 直连，无连接池）、JarHashUtil（classes 条目聚合/lib 字节流 SHA-256）、BootJarParser（ASM SKIP_CODE 收集方法名+desc，排除 clinit/synthetic）、MachineInfo（hostname+ip）、KillUtil（kill -9 → taskkill → halt 兜底）、FaultLogger（自写文件日志，不引 slf4j 避免污染 system classpath）
- fault-agent：premain 全流程（bootJar 定位兜底链 → 表1 状态机 pending/completed/failed + 本机中断判定(ip+boot_jar) → INSERT IGNORE 幂等落库 → 同步 bash sandbox.sh -p pid -d "fault-module/inject?id=..." → 超时/异常硬保护：置 failed+写表4+kill）；HardProtectException 携带 phase/errorType/unitIds
- fault-module：@Command("inject") 按 id 查表2 → 每类一条 EventWatchBuilder 链（onClass→onBehavior×N→onWatching().withLine().onWatch 共享 listener）→ beforeLine INSERT 表3 抢占（成功 kill/DuplicateKey 放行+preemptedLines 记忆）；sandbox 1.4.0 API：IBuildingForClass/IBuildingForBehavior/IBuildingForWatching，无 withLoad（watcher 常驻天然覆盖后加载类）、onBehavior(String) 单参
- 关键构建事实：agent fat jar 中 mysql/asm/protobuf 已 relocate（cn.chinaclear.fault.shaded.*）防 system classpath 冲突；JDBC url 带 connectTimeout=5000&socketTimeout=10000（硬保护及时性）
- test-app：Spring Boot 2.7.18（Java 8 release，兼容 Linux JDK 21）；**Spring Boot 2.x gradle 插件不兼容 Gradle 8** → 手工 Jar task 组装 bootJar（JarLauncher + BOOT-INF/classes/lib，nested jar STORED，Zip task 无 manifest 块必须用 Jar task）；烟测 Started ✓
- 构建环境坑：**PowerShell 命令行中文路径乱码**（用短路径 SANDBO~2=C:\Users\86132\Desktop\sandbox行故障 绕过）；GRADLE_USER_HOME=D:\JAVA\repository（不在 ~/.gradle）；mysql-connector-j 坐标是 com.mysql 前缀；gradle-wrapper distributionUrl 改腾讯镜像
- Linux 192.168.193.131（lys2/yi2000316lei，Ubuntu 20.04，OpenJDK 21）：sudo 可用；apt 装了 mysql-server（MySQL 8.0）；**待办：启动 mysql + root 改 mysql_native_password root123 + 建库 fault_sandbox**；sandbox 在 /home/lys2/sandbox（bin/sandbox.sh、sandbox-module/——注意实际目录名是 sandbox-module 不是 sandbox-modules）
- plink 非交互：A:\putty\plink.exe -batch -hostkey "SHA256:iAijTOpw4SHGN6KfZ+hB6bYfLWsBsGOAN97fTJmQiQk" -pw yi2000316lei lys2@192.168.193.131
- 待办：Linux 端到端跑 tests/TEST_CASES.md TC1-TC8
- **TC1-TC8 全部验证通过**（2026-08-30）：完整闭环=解析落库→自动挂载→启动期/运行期行命中→表3 记录→kill；TC4 验证死点前进语义；TC5 两节点各死于不同新行；TC7 表4+kill+不放行。MySQL 用 Windows 便携版（192.168.193.1:3306，root/root123 已建 root@% 远程账号），Linux 通过 agentArgs jdbc.host 覆盖
- 端到端修复的三个 bug：①JDK9+ getInputArguments() 不含 -jar（bootJar 定位改为 sun.java.command → /proc/self/cmdline → inputArguments → 配置兜底）；②sandbox.sh 必须以 sandbox/bin 为工作目录执行（SANDBOX_HOME_DIR=${PWD}/..），ProcessBuilder.directory() 修复；③mount 失败不应把已 completed 的解析单元置 failed（unitIds 传 null）
- 最终 commit 至 c8259e4；README/TEST_CASES.md 已含执行结果
- **追加功能（71deab7）**：①轮次 tag——目标应用加 `-Dfault.tag=<轮次>`，模块 inject 时读取；缺失→写表4+直接 kill（不放行）；表3 加 tag 列、唯一索引升级 uk(unit_id,class,method,line,tag)（判重仅限轮内，SchemaInitializer.migrate 幂等迁移旧库）；②lambda 命中——BootJarParser 对 synthetic 方法仅纳入 lambda$ 前缀（bridge/access$ 仍排除，clinit 仍排除）
- 追加验证：V1 无 tag kill ✓；V2 tagA 对旧空 tag 记录隔离命中 ✓；V3 tagB 渐进重启命中 lambda$startupRunner$0 与 lambda$auditAll$0 内部行 ✓；V4 tagB 轮 24 行判重无重复 ✓
- 渐进验证技巧：同 tag 重启每轮死点前进（tag 内已记录行自动放行）；远端 pkill 必须用 `pkill -x java`（-f test-app.jar 会匹配 plink 远端 shell 自身命令行导致循环自杀）；PowerShell 传 plink 时 `\$i:` 冒号语法会解析失败
